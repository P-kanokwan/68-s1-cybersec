services :
  s1app :
    image : bpage/pgadmin4
    container_name : 68-s1-admin
    ports :
      - ${PGADMIN_PORT}:80
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT}